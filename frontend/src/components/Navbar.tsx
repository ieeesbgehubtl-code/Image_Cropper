import {Camera,History,Info,Settings,SunMoon} from 'lucide-react';import {useTheme} from '../contexts/ThemeContext';
export function Navbar({page,setPage}:{page:string;setPage:(p:string)=>void}){const{toggle}=useTheme();const items=[['home',Camera],['about',Info],['settings',Settings],['history',History]] as const;return <nav className="glass m-4 flex flex-wrap items-center justify-between p-4"><h1 className="text-xl font-black text-slate-900 dark:text-white">AI Passport Photo Generator</h1><div className="flex gap-2">{items.map(([id,Icon])=><button key={id} onClick={()=>setPage(id)} className={`rounded-xl px-3 py-2 capitalize ${page===id?'bg-blue-600 text-white':'text-slate-700 dark:text-slate-200'}`}><Icon className="inline h-4"/> {id}</button>)}<button onClick={toggle} className="rounded-xl px-3 py-2 text-slate-700 dark:text-slate-200"><SunMoon/></button></div></nav>}
