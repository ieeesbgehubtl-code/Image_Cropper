# Backend

FastAPI service for the AI Passport Photo Generator. Install dependencies from `requirements.txt`, copy `.env.example` to `.env`, and run `uvicorn app.main:app --reload` from this directory. API docs are available at `/docs`.
